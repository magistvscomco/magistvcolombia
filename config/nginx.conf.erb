worker_processes 1;
events { worker_connections 1024; }
http {
    server {
        listen <%= ENV["PORT"] %>;
        location / {
            root <%= ENV["HOME"] %>/public;
            index index.html;
        }
    }
}
